# Public claims matrix

Verified on **2026-08-02**. The tested implementation baseline is
[`8039675d68e748b37a73a2796959c8d8bda8dabc`](https://github.com/NexuChat/sidq/commit/8039675d68e748b37a73a2796959c8d8bda8dabc).
The repository revision containing this matrix may be newer because evidence
records are committed after the implementation. The deployed revision is
authoritative only when the same SHA appears in `/healthz`, the landing footer,
and `origin/main`; deployment is not inferred from a Git commit.

| Public claim | Exact scope and evidence class | Source of truth / artifact | Reproduce | Revision |
|---|---|---|---|---|
| The required suite collects 1006 tests: 1004 pass and 2 optional integrations skip; total branch coverage is 83.85%. | Host run, Python 3.12.3; guaranteed by the test/coverage gate for this revision. The skips are the uncached Qwen embedding integration and unavailable local Ollama model integration. | `pyproject.toml`, `Makefile`, [`QA-RESULTS.md`](QA-RESULTS.md) | `make check`; for reasons: `.venv/bin/pytest -q -rs --no-cov` | `8039675` implementation baseline; re-run on the document revision before release |
| The committed removal of `cust_email` is `BLOCK`. | Fixture-backed deterministic replay, not a live graph query. `critical_downstream` is the blocking rule because 9 cross-team owners are recorded. `wide_blast_radius` is supporting `WARN`; `PII_Data` is sensitivity context only. | [`verdict.json`](../examples/01-blocked-pii-dashboard/verdict.json), [`pr-comment.md`](../examples/01-blocked-pii-dashboard/pr-comment.md) | `make gate-demo`; `.venv/bin/python scripts/regenerate_example_01.py --check` | Evidence commit `5addb753788935d4d1aa6a9483c28c6fc124e5c7`; policy `66f48004804c5ce02955699710466b6d58ae7a868f876a4774e548c5c15920b8` |
| The example records 16 downstream consumers and one path with 8 nodes / 7 hops. | Fixture-backed column lineage through the BI field, then entity-level chart/dashboard hops. The count alone does not block. | [`verdict.json`](../examples/01-blocked-pii-dashboard/verdict.json) | `jq '.findings[] | select(.rule_id=="critical_downstream") | .evidence[0].detail | {downstream_count,paths}' examples/01-blocked-pii-dashboard/verdict.json` | Same fixture/policy revision above |
| A read-only catalog snapshot examined all 67 `showcase-ecommerce` datasets and found 285 persisted `lineage_field_missing` contradictions concentrated in 5 target assets. | Measured catalog-only snapshot. It does not claim that the underlying source systems are wrong. A separate check recorded 29 consumed-but-unowned assets. | [`report.json`](../examples/03-catalog-truth-report/report.json), [`TRUTH-REPORT.md`](TRUTH-REPORT.md) | `jq '.scope, .summary' examples/03-catalog-truth-report/report.json`; derive the five targets with `jq '[.evidence[] | select(.kind=="lineage_field_missing") | .detail.edge.target_dataset] | unique | length' .../report.json` | Committed snapshot in the document revision |
| `sidq-mcp` exposes exactly 3 read-only agent tools. | Protocol contract for `check_change`, `verify_context`, and `search_verified`; Receipt writes are a separate CLI/operator path. | [`MCP-SERVER.md`](MCP-SERVER.md), `src/sidq/mcp_server/server.py` | `make mcp-smoke`; `pytest -q tests/test_mcp_server.py tests/test_mcp_snapshot.py` | `8039675` implementation baseline |
| The swarm demo starts 4 independent workers and deliberately kills one. | Connected operational demonstration, not a measured recovery total and not an exactly-once claim. Work may duplicate safely; the observer reads latest DataHub state. | `Makefile` target `swarm-demo`, `src/sidq/agent/swarm.py`, `tests/test_swarm.py` | `make swarm-demo` against the documented connected stack | `8039675` implementation baseline |
| Receipt success requires an exact direct readback; an unconfirmed write fails closed and attempts a bounded compensating rollback. | Guaranteed by adversarial state-machine tests. DataHub mutations remain non-transactional and have no cross-process compare-and-set, so divergent state is surfaced as `rollback_incomplete`, never reported as stored evidence. | [`RECEIPT-SPEC.md`](RECEIPT-SPEC.md), `src/sidq/receipt/write.py`, `tests/test_receipt.py` | `pytest -q tests/test_receipt.py tests/test_cli_agent_surfaces.py` | `8039675` implementation baseline |
| The public landing exposes 5 fixed demonstrations and no catalog-write flag. | Tested server allowlist; not arbitrary command execution. | `web/server.py`, `tests/test_web_security.py`, [`QA-RESULTS.md`](QA-RESULTS.md) | `pytest -q tests/test_web_security.py tests/test_landing_experience.py`; `curl https://sidq.mlki.app/healthz` | Verify against the SHA returned by public `/healthz` |
| The shipped two-term decision measured 51 ns median. | Single-machine microbenchmark of the deterministic rule only, not end-to-end MCP latency, an SLA, or field telemetry. MCP wall time varies by catalog and environment. | [`DECISION-COST.md`](DECISION-COST.md), `data/benchmark/decision-cost-gpu.json` | `.venv/bin/python scripts/measure_decision_cost.py` | Committed benchmark artifact in the document revision |
| Automated live-DOM accessibility audit reports 0 violations; the tested 375, 768, and 1440 px layouts have no horizontal overflow. | Browser measurement of the released landing, not a guarantee about every width or assistive-technology/browser combination. Keyboard focus, reduced motion, console, and network behavior are separately checked. | [`QA-RESULTS.md`](QA-RESULTS.md) | Browser QA procedure recorded there | Verify against the SHA returned by public `/healthz` |
| One controlled page-load measurement is recorded; it is not an SLA or field telemetry. | Single lab run only. No third-party tracker or real-user monitoring claim. | [`QA-RESULTS.md`](QA-RESULTS.md) | Run the Playwright performance probe described there | Verify against the SHA returned by public `/healthz` |
| The application lock resolves 125 packages with no known advisory; the isolated MCP lock retains one unique upstream setuptools advisory. | Current dependency-database result. `requirements-mcp.lock` produces two rows for the same `PYSEC-2026-3447`; DataHub's `setuptools<82` constraint excludes the fixed 83.0.0 release. The affected macOS sdist-creation path is outside the deployed Linux wheel-only runtime, but the advisory remains documented. | [`SECURITY-AUDIT.md`](SECURITY-AUDIT.md), [`SECURITY.md`](../SECURITY.md#python-dependency-advisory-status) | `uv audit --locked --no-cache`; `uvx pip-audit --disable-pip --requirement requirements-mcp.lock` | Audited 2026-08-02 on the document revision |
| The verified English-film artifact is 1920×1080/30 fps, 5,075 authored frames, with a narration-only composition. | Local upload artifact, not a public-upload claim. Container duration 169.216 seconds; exact size 29,636,338 bytes; burned English captions plus a 54-cue sidecar SRT. | [`VIDEO.md`](VIDEO.md), `/home/dev/sidq-video/artifacts/video/sidq-final-en.mp4` | `sha256sum`; `ffprobe -count_frames`; complete `ffmpeg -v error -xerror` decode; package-manifest verification | SHA-256 `0811a494c3ee6f78f907c3f2d14908ca18df403d81e38d63093cfa7dab46beef`; public URL owner-pending |
| Production serves the exact released Git SHA. | Live deployment claim, accepted only when root-owned immutable release path, public `/healthz`, footer, and `origin/main` agree. | [`QA-RESULTS.md`](QA-RESULTS.md), [`OPERATIONS.md`](OPERATIONS.md) | `curl https://sidq.mlki.app/healthz`; inspect footer; compare `git rev-parse origin/main` and `/opt/sidq/current` | Final released SHA recorded after activation |

## Interpretation rules

- `PASS`, `WARN`, and `BLOCK` are policy verdicts. `NOT VERIFIED` is a reader
  state, never a fourth verdict, and it covers exactly three cases: the receipt
  is absent, stale, or unreadable. A current receipt recording `BLOCK` is
  reported as `CURRENT RECEIPT · BLOCK · STOP`.
- Coverage and authorization are separate claims. A current receipt covers its
  asset whatever the verdict — that is what lets a bounded audit converge — while
  only a current `PASS` authorizes an agent to continue unaided. See
  [`RECEIPT-SPEC.md`](RECEIPT-SPEC.md#three-questions-three-fields).
- “Deterministic” applies to enforcement for identical canonical evidence and
  policy, not to network collection or availability.
- The structured Receipt body is the reader's sole receipt authority; it is not a
  signature, append-only ledger, or exactly-once coordination mechanism.
- “Live”, “fixture replay”, “measured once”, and “test guarantee” are not
  interchangeable; every row names its boundary.
